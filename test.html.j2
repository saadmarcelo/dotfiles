<!DOCTYPE html>
<html>
  <head>
    <title>{{ title | default("Teste") }}</title>
  </head>
  <body>
    {% for item in items %}
      <div class="item">
        <h2>
{{ item.name }}
        </h2>
        <p>
{{ item.description | truncate(100) }}
        </p>
      </div>
    {% endfor %}
  </body>
</html>
